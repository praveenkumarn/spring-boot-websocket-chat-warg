version: '3'
services:
  web:
    image: 172.31.44.24:8083/spring-boot-websocket-chat-demo:latest-v1
    ports:
      - "8040:8080"
    networks:
      - overlay
    environment:
      - SPRING_PROFILE:STG 
    deploy:
      replicas: 4

networks:
  overlay:
    external:
      name: epicnetwork
